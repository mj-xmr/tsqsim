#include "pch_test.h"
#include <UnitTest++/UnitTest++.h>

#include "InterestMan.h"

using namespace std;

//static InterestMan gIntMan;
//static string aud = "AUD";

/*
TEST(InterestMan_nonExsisting)
{
    bool result = gIntMan.HasSymbol("Dupa");
    bool expected = false;
    CHECK_EQUAL(expected, result);
}

TEST(InterestMan_Exsisting)
{
    bool result = gIntMan.HasSymbol(aud);
    bool expected = true;
    CHECK_EQUAL(expected, result);
}

TEST(InterestMan_bid_exact)
{
    float result = gIntMan.GetBid(aud, 20170622014830);
    float expected = 1.0500;
    CHECK_EQUAL(expected, result);
}

TEST(InterestMan_ask_exact)
{
    float result = gIntMan.GetAsk(aud, 20170622014830);
    float expected = 1.9500;
    CHECK_EQUAL(expected, result);
}

//AUD,1.1500,2.1500,2017-04-26 06:28:55
//AUD,1.0500,2.2500,2017-06-14 21:03:48
TEST(InterestMan_ask_between)
{
    float result = gIntMan.GetAsk(aud, 20170501000000);
    float expected = 2.1500;
    CHECK_EQUAL(expected, result);
}
*/

