#!/bin/bash -e

#if [ "$(uname)" == "Darwin" ]; then
#	HOMEBREW_NO_AUTO_UPDATE=1 brew install mkl
#else
#	sudo apt update; sudo apt -y install libmkl-dev # TODO: Enable once it makes sense

#apt-get install libmkl-dev # Needs up to 2 GB of space.
# Non interactive alternative (deprecated):
#DEBIAN_FRONTEND=noninteractive apt-get install -y -q --force-yes libmkl-dev
#fi


